scope = "city"
fallback = true
nudge = "Run gc hook --claim to claim work. After closing it, run gc hook --claim for more work."
start_gate = "gc hook --claim --start-gate"
idle_timeout = "2h"
min_active_sessions = 0
max_active_sessions = 3
